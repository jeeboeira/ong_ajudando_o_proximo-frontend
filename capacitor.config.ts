import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ong_ajudando_o_proximo-frontend',
  webDir: 'www'
};

export default config;
